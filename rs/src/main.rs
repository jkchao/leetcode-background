mod longest_substring_without_repeating_characters;
mod merge_sorted_array;
mod two_sum;

fn main() {
    let len = longest_substring_without_repeating_characters::longest_substring_without_repeating_characters("pwwkew".to_string());
    println!("{}", len);
}
